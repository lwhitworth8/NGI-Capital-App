# Optional heavy/analytics libs (install only if needed)

# Data processing
pandas>=2.2.3,<3
numpy>=2.1.1,<3
openpyxl>=3.1.2

# Analytics/DB
duckdb>=1.1.3,<2

# PDF export (optional; reports export will fallback if missing)
reportlab>=4.2.5

# Image processing (for OCR doc extraction)
pillow>=10.4.0
