// Temporary ambient module so the editor resolves @ngi/ui during development
// even if the workspace link has not been installed yet. Once you run
// `npm install` at the repo root, this file can be removed.
declare module '@ngi/ui';

